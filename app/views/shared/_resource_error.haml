- if resource.errors.any?
  #error_explanation
    %h2= "#{pluralize(resource.errors.count, "error")} prohibited this experience from being saved:"
    %ul
      - resource.errors.full_messages.first(4).each do |message|
        %li= message